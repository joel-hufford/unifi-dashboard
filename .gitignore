__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
# Anything that could hold controller credentials. config.toml is where the
# API key goes when you run from a clone instead of /etc/unifi-dashboard.
config.toml
config.local.toml
secrets.env
*.env
.env

# Controller dumps: these carry real MACs, public addresses and site names.
# Paste them into a conversation rather than committing them.
diagnostics/*.json

*.db
*.db-wal
*.db-shm
.DS_Store
